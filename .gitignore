# Build outputs
Image
kernel.iso
kernel/system
kernel/system.bin
# stray artifacts from the pre-kernel/ layout (these were once committed)
system
system.bin
boot/boot
boot/setup
tools/build

# Object files
*.o
*.a

# Temp files
.iso_tmp/
.image_floppy_padded
*~
core
.DS_Store

# MINIX test disk (generated)
minix.img
tools/mkminix

# Single self-booting disk image (task D) and the filesystem it embeds
linux.img
disk-fs.img
tools/mkdisk

# ... and its VMware/VirtualBox containers (`make vmdk`)
linux.vmdk
linux-flat.vmdk
linux-flat-flat.vmdk

# VMware's lock directory, created while a VM has one of them attached
*.lck/

# Regression test serial logs
test-logs/

# User program build outputs
user/user.bin
user/user.elf
user/user_data.c

# Auto-generated header deps (-MMD)
*.d
user/*.elf

# Python bytecode from the repo scripts
__pycache__/
*.pyc